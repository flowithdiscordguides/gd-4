# Third-Party Notices

GitDesk's selected-version Markdown preview uses locally packaged browser distributions from:

- DOMPurify 3.4.12, licensed under MPL-2.0 or Apache-2.0.
- Marked 18.0.7, licensed under the MIT License.

Run `npm install` from the repository root to copy the pinned browser bundles and their complete license texts into
the packaged UI and `third_party_licenses` directories. GitDesk does not load either dependency from a CDN.
